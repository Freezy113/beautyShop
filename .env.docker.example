# Docker Environment Variables for BeautyShop
# Скопируйте этот файл в .env и измените значения

# Database (внутри Docker сети)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=beautyshop

# Backend
NODE_ENV=production
PORT=3000
# PostgreSQL доступна внутри Docker как postgres:5432
DATABASE_URL=postgresql://postgres:your-secure-password-here@postgres:5432/beautyshop

# JWT Secret (ОБЯЗАТЕЛЬНО измените в продакшене!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Frontend (опционально, для переопределения API URL)
# VITE_API_URL=http://localhost:3000
